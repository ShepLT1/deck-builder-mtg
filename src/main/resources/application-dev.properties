spring.datasource.url=jdbc:postgresql://db:5432/mtg_deck_builder
spring.datasource.username=postgres
spring.datasource.password=postgres

# The format used for the keystore. It could be set to JKS in case it is a JKS file
server.ssl.key-store-type=PKCS12
# The path to the keystore containing the certificate
server.ssl.key-store=classpath:localhost.p12
# The password used to generate the certificate
server.ssl.key-store-password=localhost

com.mtg.allowedOrigins=https://localhost:5173

# Testing
#com.mtg.jwtExpirationMs= 60000
#com.mtg.jwtRefreshExpirationMs= 120000